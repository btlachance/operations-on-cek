#lang s-exp (submod (file "~/projects/operations-on-cek/examples/impcore/spec.rkt") impcore)
(val r 0)
(define gcd (m n)
  (begin
    (while (!= (set r (mod m n)) 0)
      (begin
        (set m n)
        (set n r)))
    n))
(gcd 6 15)
