#lang s-exp (file "~/projects/operations-on-cek/examples/impcore/impcore.rkt")
(define sum (n result)
  (if n
      (sum (- n 1) (+ n result))
      result))
(define run (n)
  (if (- n 1)
      (begin
        (sum 10000 0)
        (run (- n 1)))
      (sum 10000 0)))
(run 350000)
