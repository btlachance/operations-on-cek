#lang s-exp (file "~/projects/operations-on-cek/examples/impcore/impcore.rkt")
(define sum (n result)
  (if n
      (sum (- n 1) (+ n result))
      result))

(val iters 350000)
(while iters
  (begin
    (sum 10000 0)
    (set iters (- iters 1))))
